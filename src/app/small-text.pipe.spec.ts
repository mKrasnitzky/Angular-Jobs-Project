import { SmallTextPipe } from './small-text.pipe';

describe('SmallTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SmallTextPipe();
    expect(pipe).toBeTruthy();
  });
});
